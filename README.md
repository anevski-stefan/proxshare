# proxshare

Monorepo with a **Next.js** frontend, **Spring Boot** API and worker, **PostgreSQL**, and **RabbitMQ**, wired up with Docker Compose.

## Prerequisites

- Docker & Docker Compose

## Quick start

1. Copy env template and edit values:

   ```bash
   cp .env.example .env